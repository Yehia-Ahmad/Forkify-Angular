import { RecipeQuantityPipe } from './recipe-quantity.pipe';

describe('RecipeQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
